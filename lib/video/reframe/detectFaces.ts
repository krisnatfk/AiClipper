import type { TimedDetection } from './types';

export async function detectFaces(): Promise<TimedDetection[]> {
  throw new Error('Face detection runs through scripts/track_subject.py in this MVP.');
}
